apiVersion: v1
kind: Service
metadata:
    name: "abaks-efd1"
    labels:
        app: "abaks-efd1"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "abaks-efd1"